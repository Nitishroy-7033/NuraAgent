Add Dependancy 

uv add requests pandas

Run Code 
uv run main.py

# NuraAgent Backend

## Run the Application

### 1. As a CLI (Interactive Terminal)
uv run main.py

### 2. As an API (For UI Integration)
uv run main.py --api
